name: Build and Push Frontend Image

on:
  push:
    branches:
      - "main"
    paths:
      - "frontend/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image tag
        id: image-tag
        run: echo "tag=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          build-args: |
            VITE_API_URL=${{ secrets.VITE_API_URL }}
          tags: |
            ghcr.io/${{ github.repository }}/frontend:${{ steps.image-tag.outputs.tag }}
            ghcr.io/${{ github.repository }}/frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      # TODO: update path before enabling
      # - name: Update GitOps repo with new frontend image tag
      #   run: |
  #         git config --global user.email "actions@github.com"
  #         git config --global user.name "GitHub Actions"
  #         git clone https://x-access-token:${{ secrets.GH_PAT }}@github.com/huuphuc-nguyen/gitops-infras.git
  #         cd gitops-infras
  #         yq eval ".frontend.image.tag = \"${{ steps.image-tag.outputs.tag }}\"" -i projects/wordle/values.yaml
  #         git add .
  #         git commit -m "cicd: update frontend image tag to ${{ steps.image-tag.outputs.tag }}"
  #         git push
